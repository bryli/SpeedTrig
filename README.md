## To Get Started

Go to directory:
`cd ~/download_location/SpeedTrig/`

Change requirements for running req script:
`chmod +x reqs.sh`

Run req script:
`./reqs.sh`

 *Or just copy:*

```
chmod +x reqs.sh
./reqs.sh
```
